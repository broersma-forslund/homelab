apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dependency-track-db
spec:
  instances: 1
  bootstrap:
    initdb:
      database: deptrack
      owner: deptrack
  storage:
    size: 8Gi
