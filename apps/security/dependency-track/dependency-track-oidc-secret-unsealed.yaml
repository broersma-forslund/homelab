apiVersion: v1
kind: Secret
metadata:
  name: dependency-track-oidc-secret
  namespace: dependency-track
stringData:
  # Fill these values locally, then seal this secret with kubeseal before merge.
  client-id:
  client-secret:
