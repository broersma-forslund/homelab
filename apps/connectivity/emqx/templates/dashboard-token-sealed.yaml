apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: emqx-dashboard-token
  namespace: emqx
spec:
  encryptedData:
    # echo -n "admin:dashboard-token" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="emqx-dashboard-token" -n emqx | clip.exe
    # The format should be "username:password" for basic auth
    username: PLACEHOLDER_TO_BE_REPLACED
    password: PLACEHOLDER_TO_BE_REPLACED
