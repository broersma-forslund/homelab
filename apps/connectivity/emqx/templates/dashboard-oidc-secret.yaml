apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: emqx-dashboard-oidc-secret
  namespace: emqx
spec:
  encryptedData:
    # echo -n "client-secret" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="emqx-dashboard-oidc-secret" -n emqx | clip.exe
    client-secret: PLACEHOLDER_TO_BE_REPLACED
