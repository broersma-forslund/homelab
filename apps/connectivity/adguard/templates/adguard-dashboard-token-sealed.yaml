apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: adguard-dashboard-token
  namespace: adguard
spec:
  encryptedData:
    # echo -n "admin" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="adguard-dashboard-token" -n adguard | clip.exe
    # echo -n "password" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="adguard-dashboard-token" -n adguard | clip.exe
    # The basicAuth expects username and password as separate keys
    username: PLACEHOLDER_TO_BE_REPLACED
    password: PLACEHOLDER_TO_BE_REPLACED
