apiVersion: v1
kind: ConfigMap
metadata:
  name: velero-node-agent-config
data:
  velero-node-agent-config.json: |
    {
      "backupPVC": {
        "ceph-block": {
          "readOnly": true
        },
        "ceph-filesystem": {
          "readOnly": true
        }
      }
    }
