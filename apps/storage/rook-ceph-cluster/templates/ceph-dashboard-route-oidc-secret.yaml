apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ceph-dashboard-route-oidc-secret
  namespace: rook-ceph
spec:
  encryptedData:
    # echo -n "client-secret" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="ceph-dashboard-route-oidc-secret" -n rook-ceph | clip.exe
    client-secret: PLACEHOLDER_FOR_ENCRYPTED_SECRET
