intel-gpu-resource-driver-chart:

  tolerations:
    - key: node-role.kubernetes.io/gpu
      operator: Exists

  cdi:
    staticPath: /run/cdi

  nodeFeatureRules:
    enabled: true
  kubeletPlugin:
    healthMonitoring:
      enabled: false
