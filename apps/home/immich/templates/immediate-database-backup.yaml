apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: immich-data-immediate
spec:
  cluster:
    name: immich-data
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io